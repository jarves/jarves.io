<div class="documentation-navigation-sub">

{% for item in items %}
    <div class="item">
        <a class="{% if item.active %}active{% endif %}" href="{{ item.path }}">
            {{  item.title }}
        </a>
        <span class="desc">&lt;{{ item.rootName }}&gt;</span>
    </div>
{% endfor %}

</div>